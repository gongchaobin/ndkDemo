apply plugin: 'groovy'
apply plugin: 'maven'

dependencies {
    compile gradleApi()
    compile localGroovy()
    compile fileTree(dir: 'libs', include: ['*.jar'])
}

uploadArchives{
    repositories {
        mavenLocal()
    }
}

group = 'com.zxy.plugin'
version = '1.0.0'